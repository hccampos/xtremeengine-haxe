package xtremeengine;

/**
 * Interface which defines all the basic functionality that an core plugin must implement.
 *
 * @author Hugo Campos <hcfields@gmail.com> (www.hccampos.net)
 */
interface ICorePlugin extends ICoreObject extends INamed extends IInitializable {}